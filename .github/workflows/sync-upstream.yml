name: sync-upstream

on:
  # Automatic triggers are disabled in this fork; run manually from
  # the Actions tab, or restore the triggers this commit removed.
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    name: Sync from aresbit/autoagent-design
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0

      - name: Merge upstream/main into main
        run: |
          git remote add upstream https://github.com/aresbit/autoagent-design.git
          git fetch upstream main
          git checkout main
          git merge upstream/main --no-edit
          git push origin main
